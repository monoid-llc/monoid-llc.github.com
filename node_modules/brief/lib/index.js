// Generated by CoffeeScript 1.4.0
var existsSync, fs, mod, modName, path;

path = require('path');

fs = require('fs');

existsSync = (fs.existsSync ? fs : path).existsSync;

modName = path.basename(path.resolve(__dirname + '/..'));

if (existsSync(__dirname + '/../src')) {
  require('coffee-script');
  mod = require("../src/" + modName);
} else {
  mod = require("./" + modName);
}

mod.version = require('../package.json').version;

module.exports = mod;
