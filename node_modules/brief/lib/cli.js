// Generated by CoffeeScript 1.4.0
var brief, help, program, version;

program = require('jade/node_modules/commander');

version = require('../package.json').version;

brief = require('./index');

program.version(version).usage('-i <input> -o <output> -t <template>').option('-i, --input <file>', 'markdown file to use as input').option('-o, --output <file>', 'where to output rendered content').option('-t, --template <file>', 'jade template to use').parse(process.argv);

help = function() {
  console.log(program.helpInformation());
  return process.exit();
};

if (!program.template || !program.input || !program.output) {
  help();
}

brief.renderFile(program.template, program.input, program.output);
